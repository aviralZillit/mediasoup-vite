[build]
  base = "app"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  VITE_NODE_ENV = "dev"
  NODE_VERSION = "18"

# SPA routing — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
